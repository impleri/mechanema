import { createStateMachine } from './state-machine';

describe('state machine functions', () => {
  describe('Initial constants', () => {
    it('are exported', () => { });
  });

  describe('createStateMachine', () => {
    it('builds a state machine reducer function', () => { });
  });
});
